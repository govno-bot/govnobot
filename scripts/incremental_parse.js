const fs=require('fs'); const s=fs.readFileSync('src/commands/command-handler.js','utf8'); let acc=''; const lines=s.split(/\r?\n/); for(let i=0;i<lines.length;i++){ acc += lines[i] + '\n'; try{ new Function(acc); } catch(e){ console.error('parse error at line', i+1, 'message:', e.message); console.error('snippet:'); console.error(lines.slice(Math.max(0,i-5), i+2).join('\n')); process.exit(0); } } console.log('no parse errors');